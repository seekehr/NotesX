function Sidepanel() {
    const closePanel = () => {
        
    };

    return (
        <div className="sidepanel">
            <a href="javascript:void(0)" className="closebtn" onClick={closePanel}>&times;</a>
        </div>
    );
}

export default Sidepanel;